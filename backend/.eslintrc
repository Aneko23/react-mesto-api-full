{
  "extends": "airbnb-base",
  "rules": {
    "no-underscore-dangle" : ["error", { "allow": ["_id"] }],
    "object-shorthand": "error",
    "no-unused-vars": ["error", {"var": "all", "args": "all" }]
  }
}